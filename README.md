start installing expo and node
set gitignore
always get into the directory and use npm install to install all the dependencies.
add required api keys and info to env files
set the ip according to the systems local ip before starting the frontend.